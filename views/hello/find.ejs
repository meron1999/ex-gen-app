<!DOCTYPE html>
<html lang="ja">
<head>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <link type="text/css" rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" crossorigin="anonymous">
    <title><%=title %></title>
</head>

<body class="container">
<header>
    <h1 class="display-4"><%=title %></h1>
</header>
<div role="main">
    <p><%- content %></p>
    <form method='post' action='/hello/find'>
        <div class="form-group">
            <label for="find">FIND</label>
            <input type="text" class="form-control" id="find" name="find" value="<%=find %>" placeholder="検索条件を入力してください">
        </div>
    </form>

    <table class="table mt-4">
        <thead>
            <tr>
                <th>ID</th>
                <th>NAME</th>
                <th>MAIL</th>
                <th>AGE</th>
            </tr>
        </thead>
        <tbody>
            <% mydata.forEach(function(data) { %>
                <tr>
                    <td><%= data.id %></td>
                    <td><%= data.name %></td>
                    <td><%= data.mail %></td>
                    <td><%= data.age %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</div>
</body>
</html>